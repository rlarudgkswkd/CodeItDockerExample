spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/menudb
    username: menuuser
    password: menupass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: true

server:
  port: 8080